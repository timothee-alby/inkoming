import React from 'react'
import HelpIcon from '@material-ui/icons/Help'

const CardUnknownIcon = ({ colour, className }) => {
  return <HelpIcon className={className} />
}

export default CardUnknownIcon
